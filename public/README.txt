Files placed in this folder are copied to the root of the build as-is.

Kamlendu_Kumar_Resume.pdf
    Drop the PDF here with exactly this filename and the "Résumé (PDF)" button
    appears on the final chapter. Until then the button removes itself rather
    than shipping a link that 404s in front of a recruiter — see
    src/ui/Content.js, _contactBlock().

Anything else you add here (an og:image, a favicon, a /robots.txt) is served
from the site root with no configuration.
